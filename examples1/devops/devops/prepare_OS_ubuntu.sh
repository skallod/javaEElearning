#prepare OS udemy 3-15:17
##apt-get purge package-name #delete with config file
##apt-get remove package-name #delete without config file
apt-get update #update package list
#apt-get upgrade #upgrade packages [manual] Y/n try !!!!! eval - auto manual input
#reboot #activate changes
#apt-get update
#apt-get upgrade #the following packages 3 not upgraded
##apt-get dist-upgrade # WARN (not use) setup but config may will be broken (overwrite configs)
#apt-get install package-names #good choice [manual]
#apt-get update
#apt-get upgrade #0 to upgrade
#apt-get autoremove #remove unneccery [manual]
#update-grub #reload bootlaoder
##echo "127.0.0.1 siteup.local" >>/etc/hosts
#sudo ufw enable